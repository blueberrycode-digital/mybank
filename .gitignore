# Xcode
#
build/
DerivedData/
*.hmap
*.ipa
*.xcuserstate

# Xcode 8 and later
*.xccheckout
*.xcscmblueprint

# Swift Package Manager
#
# Add this line if you want to avoid checking in source code from Swift Package Manager dependencies.
# Packages/
# Add this line if you want to avoid checking in build products from Swift Package Manager.
.build/

# CocoaPods
#
# We recommend against adding the Pods directory to your .gitignore. However
# you should judge for yourself, the pros and cons are mentioned at:
# https://guides.cocoapods.org/using/using-cocoapods.html#source-control
Pods/
# Add this line if you want to avoid checking in source code from the Xcode workspace
# when using CocoaPods.
xcuserdata/

# Carthage
#
# Add this line if you want to avoid checking in source code from Carthage dependencies.
Carthage/Checkouts
# Add this line if you want to avoid checking in build products from Carthage.
Carthage/Build

# Fastlane
#
# It is recommended to not store the screenshots in the git repo. Instead, use fastlane to re-generate the
# screenshots whenever they are needed.
*/fastlane/report.xml
*/fastlane/Preview.html
*/fastlane/screenshots
*/fastlane/test_output
# Uncomment the line below if you want to avoid checking in the `fastlane` folder.
# fastlane/

# Code Injection by AppCode
#
# Uncomment the line below if you want to avoid checking in AppCode's generated files.
# appcodeinject/

# Other
#
# Add this line if you want to avoid checking in Xcode's user-specific settings.
xcuserdata/
*.xcuserdatad
*.xcuserstate
*.xcworkspace
*.moved-aside
*.ideworkspace
*.xcdebugger
*.xcsettings

# Add this line if you want to avoid checking in Playgrounds
timeline.xctimeline
playground.xcworkspace
*.playgroundbook

# Add this line if you want to avoid checking in Xcode archive files
*.xcarchive

# Add this line if you want to avoid checking in device logs
*.deviceconsole

# Add this line if you want to avoid checking in OS X Finder metadata files
.DS_Store

# Add this line if you want to avoid checking in Finder resource fork files
.AppleDouble

# Add this line if you want to avoid checking in Spotlight files
.Spotlight-V100

# Add this line if you want to avoid checking in VIM swap files
.*.swp

# Add this line if you want to avoid checking in OS X Trash folder files
.Trashes

# Add this line if you want to avoid checking in shell script backups
*~.nib
*~.pbxuser
*~.mode1v3
*~.mode2v3
*~.perspectivev3
*~.perspectivev3~previous

# Add this line if you want to avoid checking in the project's build logs
*.log

# Add this line if you want to avoid checking in the project's index data
*.xcindex

# Add this line if you want to avoid checking in the project's scheme data
*.xcscheme

# Add this line if you want to avoid checking in Xcode server files
*.xccov

# Add this line if you want to avoid checking in Xcode's source control logs
*.xcscm

# Add this line if you want to avoid checking in Xcode's automated device deployment files
*.xcdev*

# Add this line if you want to avoid checking in iOS simulator devices and settings
.DerivedData
*Debug-iphonesimulator
*Release-iphonesimulator

# Add this line if you want to avoid checking in files generated by the REPL
*.repl

# Add this line if you want to avoid checking in files generated by the Previews feature
*.preview

# Add this line if you want to avoid checking in Xcode's intermediate build files
*.xcintermediate

# Add this line if you want to avoid checking in Xcode's derived information files
*.xcodeindex
